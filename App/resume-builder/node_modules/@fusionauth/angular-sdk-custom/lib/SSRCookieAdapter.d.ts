import { CookieAdapter } from '../sdkcore';
/** An adapter class that supports accessing cookies with SSR */
export declare class SSRCookieAdapter implements CookieAdapter {
    private isBrowser;
    constructor(isBrowser: boolean);
    at_exp(cookieName?: string): string | -1 | undefined;
}
